<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="90dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/list_item_photo"
        android:layout_width="80dp"
        android:layout_height="60dp"
        android:layout_marginStart="28dp"
        android:layout_marginTop="12dp"
        android:src="@drawable/ic_launcher_background"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

    <TextView
        android:id="@+id/list_record_name"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="128dp"
        android:layout_marginTop="22dp"
        android:text="record name"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/list_record_time"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="time"
        app:layout_constraintStart_toStartOf="@id/list_record_name"
        app:layout_constraintTop_toBottomOf="@id/list_record_name"
        tools:layout_editor_absoluteX="30dp"
        tools:layout_editor_absoluteY="52dp" />

    <Button
        android:id="@+id/list_item_edit"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_marginStart="0dp"
        app:layout_constraintStart_toEndOf="@id/list_record_name"
        app:layout_constraintTop_toTopOf="@id/list_record_name"
        android:text="修改"/>
</androidx.constraintlayout.widget.ConstraintLayout>